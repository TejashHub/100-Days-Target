#include<iostream>
#include<cmath>
#define END endl << endl

using namespace std;

int main(){
    double base = 5, exponent = 2;
    cout << base << "^" << exponent << " = " << pow(base, exponent) << END;
}